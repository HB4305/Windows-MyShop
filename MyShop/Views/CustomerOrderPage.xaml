<Page x:Class="MyShop.Views.CustomerOrderPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:MyShop.Views"
      xmlns:m="using:MyShop.Models"
      xmlns:vm="using:MyShop.ViewModels"
      xmlns:conv="using:MyShop.Converters"
      Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

  <Page.Resources>
    <conv:StringToVisibilityConverter x:Key="StringToVisibilityConverter"/>
    <conv:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    <conv:InverseBoolConverter x:Key="InverseBoolConverter"/>

    <DataTemplate x:Key="CustomerOrderItemTemplate" x:DataType="m:CustomerOrder">
      <StackPanel Padding="8">
        <TextBlock Text="{x:Bind CustomerName}" FontWeight="SemiBold" FontSize="16"/>
        <TextBlock Text="{x:Bind CustomerPhone}" Foreground="Gray" FontSize="13"/>
        <StackPanel Orientation="Horizontal" Spacing="8" Margin="0,4,0,0">
          <TextBlock Text="{x:Bind Status}" FontSize="13"/>
          <TextBlock Text="|" Foreground="Gray" FontSize="13"/>
          <TextBlock Text="{x:Bind PaymentStatus}" FontSize="13"/>
          <TextBlock Text="|" Foreground="Gray" FontSize="13"/>
          <TextBlock Text="{x:Bind TotalAmount}" FontSize="13"/>
        </StackPanel>
      </StackPanel>
    </DataTemplate>
  </Page.Resources>

  <Grid Margin="24">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <!-- Title + Progress -->
    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
      <TextBlock Text="Đơn hàng" FontSize="28" FontWeight="Bold" VerticalAlignment="Center"/>
      <ProgressBar IsIndeterminate="True" Width="120" Margin="16,0,0,0"
                   VerticalAlignment="Center"
                   Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}"/>
    </StackPanel>

    <!-- Reload button + Count -->
    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
      <Button Content="Tải lại" Command="{Binding LoadOrdersCommand}"
              IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBoolConverter}}"/>
      <TextBlock Margin="16,0,0,0" VerticalAlignment="Center">
        <Run Text="{Binding Orders.Count, Mode=OneWay}"/>
        <Run Text=" đơn hàng"/>
      </TextBlock>
    </StackPanel>

    <!-- List -->
    <ListView Grid.Row="2"
              ItemsSource="{Binding Orders}"
              ItemTemplate="{StaticResource CustomerOrderItemTemplate}"
              BorderThickness="1" BorderBrush="Gray">
      <ListView.ItemContainerStyle>
        <Style TargetType="ListViewItem">
          <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        </Style>
      </ListView.ItemContainerStyle>
    </ListView>

    <!-- Error -->
    <TextBlock Grid.Row="3" Text="{Binding ErrorMessage}"
               Foreground="Red" TextWrapping="Wrap" Margin="0,12,0,0"
               Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}"/>
  </Grid>
</Page>