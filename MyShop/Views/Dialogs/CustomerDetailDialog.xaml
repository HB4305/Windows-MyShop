<ContentDialog
    x:Class="MyShop.Views.Dialogs.CustomerDetailDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:conv="using:MyShop.Converters"
    xmlns:m="using:MyShop.Models"
    mc:Ignorable="d"
    x:Name="DialogRoot"
    MinWidth="600"
    MaxWidth="800">

    <ContentDialog.Resources>
        <conv:CurrencyFormatter x:Key="CurrencyFormatter"/>
        <conv:DateTimeOffsetConverter x:Key="DateTimeOffsetConverter"/>
        <conv:OrderStatusToBrushConverter x:Key="OrderStatusToBrushConverter"/>
        <conv:OrderStatusToForegroundConverter x:Key="OrderStatusToForegroundConverter"/>
        <conv:PaymentStatusToBrushConverter x:Key="PaymentStatusToBrushConverter"/>
        <conv:PaymentStatusToForegroundConverter x:Key="PaymentStatusToForegroundConverter"/>
        <conv:OrderIdConverter x:Key="OrderIdConverter"/>

        <Style x:Key="SectionTitle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{ThemeResource TextFillColorTertiary}"/>
            <Setter Property="CharacterSpacing" Value="50"/>
        </Style>

        <Style x:Key="DetailLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="{ThemeResource TextFillColorSecondary}"/>
        </Style>

        <Style x:Key="DetailValue" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{ThemeResource TextFillColorPrimary}"/>
        </Style>
    </ContentDialog.Resources>

    <Grid Margin="0,-12,0,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Padding="0,16,0,20">
            <Grid ColumnSpacing="16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Width="64" Height="64" CornerRadius="32" Background="#F3F0FF">
                    <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE77B;" FontSize="28" Foreground="#7C3AED" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>

                <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="4">
                    <TextBlock Text="{x:Bind Customer.Name}" FontSize="24" FontWeight="Bold" Foreground="{ThemeResource TextFillColorPrimary}"/>
                    <StackPanel Orientation="Horizontal" Spacing="12">
                        <StackPanel Orientation="Horizontal" Spacing="4">
                            <FontIcon Glyph="&#xE717;" FontSize="12" Foreground="{ThemeResource TextFillColorSecondary}"/>
                            <TextBlock Text="{x:Bind Customer.Phone}" FontSize="13" Foreground="{ThemeResource TextFillColorSecondary}"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Spacing="4">
                            <FontIcon Glyph="&#xE707;" FontSize="12" Foreground="{ThemeResource TextFillColorSecondary}"/>
                            <TextBlock Text="{x:Bind Customer.Address}" FontSize="13" Foreground="{ThemeResource TextFillColorSecondary}" TextTrimming="CharacterEllipsis" MaxWidth="300"/>
                        </StackPanel>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Stats Row -->
        <Border Grid.Row="1" Margin="0,0,0,24" Background="{ThemeResource ControlFillColorDefaultBrush}" CornerRadius="12" Padding="20,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Spacing="4">
                    <TextBlock Text="TOTAL SPENT" Style="{StaticResource SectionTitle}"/>
                    <TextBlock Text="{x:Bind Customer.TotalSpent, Converter={StaticResource CurrencyFormatter}}" FontSize="20" FontWeight="Bold" Foreground="#7C3AED"/>
                </StackPanel>

                <Border Grid.Column="1" Width="1" Background="{ThemeResource ControlStrokeColorDefaultBrush}" Margin="20,4"/>

                <StackPanel Grid.Column="2" Spacing="4">
                    <TextBlock Text="TOTAL ORDERS" Style="{StaticResource SectionTitle}"/>
                    <TextBlock Text="{x:Bind Customer.OrderCount}" FontSize="20" FontWeight="Bold" Foreground="{ThemeResource TextFillColorPrimary}"/>
                </StackPanel>

                <Border Grid.Column="3" Width="1" Background="{ThemeResource ControlStrokeColorDefaultBrush}" Margin="20,4"/>

                <StackPanel Grid.Column="4" Spacing="4">
                    <TextBlock Text="CUSTOMER SINCE" Style="{StaticResource SectionTitle}"/>
                    <TextBlock Text="{x:Bind Customer.CreatedAt, Converter={StaticResource DateTimeOffsetConverter}}" FontSize="14" FontWeight="SemiBold" Foreground="{ThemeResource TextFillColorSecondary}" VerticalAlignment="Center" Margin="0,4,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Order History -->
        <StackPanel Grid.Row="2" Spacing="12">
            <TextBlock Text="ORDER HISTORY" Style="{StaticResource SectionTitle}"/>
            
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="12" MinHeight="200" MaxHeight="300">
                <Grid>
                    <ListView ItemsSource="{x:Bind Orders}" SelectionMode="None" Padding="0,4">
                        <ListView.Header>
                            <Border BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}" BorderThickness="0,0,0,1" Padding="16,8" Background="{ThemeResource ControlFillColorDefaultBrush}">
                                <Grid ColumnSpacing="12">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="80"/>
                                        <ColumnDefinition Width="140"/>
                                        <ColumnDefinition Width="100"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="ID" FontSize="11" FontWeight="SemiBold" Foreground="{ThemeResource TextFillColorSecondary}"/>
                                    <TextBlock Grid.Column="1" Text="DATE" FontSize="11" FontWeight="SemiBold" Foreground="{ThemeResource TextFillColorSecondary}"/>
                                    <TextBlock Grid.Column="2" Text="STATUS" FontSize="11" FontWeight="SemiBold" Foreground="{ThemeResource TextFillColorSecondary}"/>
                                    <TextBlock Grid.Column="3" Text="TOTAL" FontSize="11" FontWeight="SemiBold" Foreground="{ThemeResource TextFillColorSecondary}" TextAlignment="Right"/>
                                </Grid>
                            </Border>
                        </ListView.Header>
                        <ListView.ItemTemplate>
                            <DataTemplate x:DataType="m:CustomerOrder">
                                <Border BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}" BorderThickness="0,0,0,1" Padding="16,10">
                                    <Grid ColumnSpacing="12">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="80"/>
                                            <ColumnDefinition Width="140"/>
                                            <ColumnDefinition Width="100"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="{x:Bind Id, Converter={StaticResource OrderIdConverter}}" FontSize="12" FontWeight="SemiBold" Foreground="#7C3AED" VerticalAlignment="Center"/>
                                        <TextBlock Grid.Column="1" Text="{x:Bind CreatedAt, Converter={StaticResource DateTimeOffsetConverter}}" FontSize="12" Foreground="{ThemeResource TextFillColorSecondary}" VerticalAlignment="Center"/>
                                        <Border Grid.Column="2" CornerRadius="12" Padding="8,2" HorizontalAlignment="Left" Background="{x:Bind Status, Converter={StaticResource OrderStatusToBrushConverter}}">
                                            <TextBlock Text="{x:Bind Status}" FontSize="10" FontWeight="SemiBold" Foreground="{x:Bind Status, Converter={StaticResource OrderStatusToForegroundConverter}}"/>
                                        </Border>
                                        <TextBlock Grid.Column="3" Text="{x:Bind TotalAmount, Converter={StaticResource CurrencyFormatter}}" FontSize="13" FontWeight="Bold" Foreground="{ThemeResource TextFillColorPrimary}" VerticalAlignment="Center" TextAlignment="Right"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>

                    <!-- Empty State -->
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Spacing="8" Visibility="{x:Bind Orders.Count, Mode=OneWay, Converter={StaticResource CountToVisibilityInverseConverter}}">
                        <TextBlock Text="No orders found for this customer." Foreground="{ThemeResource TextFillColorTertiary}" FontSize="13"/>
                    </StackPanel>
                </Grid>
            </Border>
        </StackPanel>

        <!-- Footer Actions -->
        <Border Grid.Row="3" Margin="0,24,0,0">
            <Button Content="Close" Click="CloseBtn_Click" HorizontalAlignment="Right" Style="{StaticResource DefaultButtonStyle}" Padding="24,8" CornerRadius="8"/>
        </Border>
    </Grid>
</ContentDialog>
