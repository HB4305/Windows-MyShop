<ContentDialog
	x:Class="MyShop.Views.Dialogs.ConfirmationDialog"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:conv="using:MyShop.Converters"
	mc:Ignorable="d"
	x:Name="DialogRoot"
	DefaultButton="Close"
	Width="440"
    Style="{StaticResource DefaultContentDialogStyle}">

	<ContentDialog.Resources>
		<conv:StringToVisibilityConverter x:Key="StringToVisibilityConverter"/>
		<CornerRadius x:Key="OverlayCornerRadius">12</CornerRadius>
	</ContentDialog.Resources>

	<StackPanel Spacing="20" Margin="4,0,4,0" Width="420">

		<!-- Custom header with warning icon -->
		<StackPanel Spacing="16">
			<!-- Warning icon circle -->
			<Border Background="{ThemeResource SystemFillColorCriticalBackgroundBrush}"
					CornerRadius="20"
					Width="48" Height="48"
					HorizontalAlignment="Left">
				<TextBlock FontFamily="Segoe MDL2 Assets"
						   Text="{x:Bind IconGlyph, Mode=OneWay}"
						   FontSize="20"
						   Foreground="{ThemeResource SystemFillColorCriticalBrush}"
						   HorizontalAlignment="Center"
						   VerticalAlignment="Center"/>
			</Border>

			<TextBlock Text="{x:Bind DialogTitle, Mode=OneWay}"
					   FontSize="18"
					   FontWeight="Bold"
					   Foreground="{ThemeResource TextFillColorPrimary}" FontFamily="ms-appx:///Assets/Fonts/MomoTrustSans-VariableFont_wght.ttf#Momo Trust Sans"/>
		</StackPanel>

		<TextBlock Text="{x:Bind DialogContent, Mode=OneWay}"
				   FontSize="13"
				   Foreground="{ThemeResource TextFillColorSecondary}"
				   TextWrapping="Wrap"
				   LineHeight="20" FontFamily="ms-appx:///Assets/Fonts/MomoTrustSans-VariableFont_wght.ttf#Momo Trust Sans"/>

		<!-- Divider -->
		<Border Background="{ThemeResource ControlStrokeColorDefaultBrush}" Height="1" Margin="-4,0,-4,0"/>

		<!-- Action buttons -->
		<Grid ColumnSpacing="10">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*"/>
				<ColumnDefinition Width="*"/>
			</Grid.ColumnDefinitions>

			<!-- Cancel button -->
			<Button Grid.Column="0"
					x:Name="CancelBtn"
					Click="CancelBtn_Click"
					HorizontalAlignment="Stretch"
					Background="{ThemeResource ControlFillColorDefaultBrush}"
					BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}"
					BorderThickness="1"
					CornerRadius="20"
					Padding="14,10"
                  Style="{StaticResource ColorPreservingButtonStyle}">
				<TextBlock Text="Cancel"
						   FontSize="13"
						   FontWeight="SemiBold"
						   Foreground="{ThemeResource TextFillColorPrimary}"
						   HorizontalAlignment="Center" FontFamily="ms-appx:///Assets/Fonts/MomoTrustSans-VariableFont_wght.ttf#Momo Trust Sans"/>
			</Button>

			<!-- Confirm (destructive) button -->
			<Button Grid.Column="1"
					x:Name="ConfirmBtn"
					Click="ConfirmBtn_Click"
					HorizontalAlignment="Stretch"
					Background="{ThemeResource SystemFillColorCriticalBrush}"
					BorderBrush="{ThemeResource SystemFillColorCriticalBrush}"
					BorderThickness="0"
					CornerRadius="20"
					Padding="14,10"
                   Style="{StaticResource ColorPreservingButtonStyle}">
				<Button.Resources>
					<SolidColorBrush x:Key="ButtonBackgroundPointerOver" Color="#E6DC2626"/>
					<SolidColorBrush x:Key="ButtonBorderBrushPointerOver" Color="#E6DC2626"/>
					<SolidColorBrush x:Key="ButtonForegroundPointerOver" Color="White"/>
				</Button.Resources>
				<StackPanel Orientation="Horizontal" Spacing="6" HorizontalAlignment="Center">
					<TextBlock FontFamily="Segoe MDL2 Assets" Text="{x:Bind IconGlyph, Mode=OneWay}" FontSize="12" Foreground="White" VerticalAlignment="Center"/>
					<TextBlock Text="{x:Bind ConfirmButtonText, Mode=OneWay}"
							   FontSize="13"
							   FontWeight="SemiBold"
							   Foreground="White" FontFamily="ms-appx:///Assets/Fonts/MomoTrustSans-VariableFont_wght.ttf#Momo Trust Sans"/>
				</StackPanel>
			</Button>
		</Grid>
	</StackPanel>
</ContentDialog>
