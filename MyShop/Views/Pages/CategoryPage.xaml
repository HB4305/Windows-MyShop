<Page x:Class="MyShop.Views.CategoryPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:MyShop.Views"
      xmlns:m="using:MyShop.Models"
      xmlns:vm="using:MyShop.ViewModels"
      xmlns:conv="using:MyShop.Converters"
      Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

  <Page.Resources>
    <conv:StringToVisibilityConverter x:Key="StringToVisibilityConverter"/>
    <conv:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    <conv:InverseBoolConverter x:Key="InverseBoolConverter"/>

    <DataTemplate x:Key="CategoryItemTemplate" x:DataType="m:Category">
      <StackPanel Padding="8">
        <TextBlock Text="{x:Bind Name}" FontWeight="SemiBold" FontSize="16"/>
        <TextBlock Text="{x:Bind Description}" Foreground="Gray" FontSize="13"
                   Visibility="{Binding Description, Converter={StaticResource StringToVisibilityConverter}}"/>
      </StackPanel>
    </DataTemplate>
  </Page.Resources>

  <Grid Margin="24">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <!-- Tiêu đề + Progress -->
    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
      <TextBlock Text="Danh mục" FontSize="28" FontWeight="Bold" VerticalAlignment="Center"/>
      <ProgressBar IsIndeterminate="True" Width="120" Margin="16,0,0,0"
                   VerticalAlignment="Center"
                   Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}"/>
    </StackPanel>

    <!-- Nút Tải lại -->
    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
      <Button Content="Tải lại" Command="{Binding LoadCategoriesCommand}"
              IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBoolConverter}}"/>
      <!-- Số lượng -->
      <TextBlock Margin="16,0,0,0" VerticalAlignment="Center">
        <Run Text="{Binding Categories.Count, Mode=OneWay}"/>
        <Run Text=" danh mục"/>
      </TextBlock>
    </StackPanel>

    <!-- Danh sách -->
    <ListView Grid.Row="2"
              ItemsSource="{Binding Categories}"
              ItemTemplate="{StaticResource CategoryItemTemplate}"
              BorderThickness="1" BorderBrush="Gray">
      <ListView.ItemContainerStyle>
        <Style TargetType="ListViewItem">
          <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        </Style>
      </ListView.ItemContainerStyle>
    </ListView>

    <!-- Thông báo lỗi -->
    <TextBlock Grid.Row="3" Text="{Binding ErrorMessage}"
               Foreground="Red" TextWrapping="Wrap" Margin="0,12,0,0"
               Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}"/>
  </Grid>
</Page>
