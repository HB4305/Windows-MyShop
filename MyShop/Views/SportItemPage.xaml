<Page x:Class="MyShop.Views.SportItemPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:m="using:MyShop.Models"
      xmlns:conv="using:MyShop.Converters"
      Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

  <Page.Resources>
    <conv:StringToVisibilityConverter x:Key="StringToVisibilityConverter"/>
    <conv:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    <conv:InverseBoolConverter x:Key="InverseBoolConverter"/>
    <conv:CurrencyFormatter x:Key="CurrencyFormatter"/>
    <conv:FirstImageConverter x:Key="FirstImageConverter"/>
  </Page.Resources>

  <Grid Margin="24">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <!-- Tiêu đề + Progress -->
    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
      <TextBlock Text="Sản phẩm" FontSize="28" FontWeight="Bold" VerticalAlignment="Center"/>
      <ProgressBar IsIndeterminate="True" Width="120" Margin="16,0,0,0"
                   VerticalAlignment="Center"
                   Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}"/>
    </StackPanel>

    <!-- Lọc & Tìm kiếm -->
    <StackPanel Grid.Row="1" Spacing="12" Margin="0,0,0,12">
    <Grid ColumnSpacing="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" /> <ColumnDefinition Width="*" /> <ColumnDefinition Width="Auto" /> </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Tìm kiếm:" VerticalAlignment="Center" FontWeight="SemiBold"/>
            <TextBox Grid.Column="1"
                     PlaceholderText="Tên sản phẩm..."
                     Text="{Binding SearchKeyword, Mode=TwoWay}"
                     HorizontalAlignment="Stretch"/>
        </Grid>

        <Grid Grid.Column="1" ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Giá:" VerticalAlignment="Center" FontWeight="SemiBold"/>
            <TextBox x:Name="MinPriceBox" Grid.Column="1" PlaceholderText="Giá từ (VND)" HorizontalAlignment="Stretch" TextChanged="OnPriceTextChanged"/>
            <TextBox x:Name="MaxPriceBox" Grid.Column="2" PlaceholderText="Giá đến (VND)" HorizontalAlignment="Stretch" TextChanged="OnPriceTextChanged"/>
        </Grid>

        <Button Grid.Column="2" Content="Tìm" Command="{Binding SearchCommand}"
                Style="{StaticResource AccentButtonStyle}" Width="80"/>
    </Grid>

    <Grid>
        <StackPanel Orientation="Horizontal" Spacing="12" HorizontalAlignment="Left">
            <TextBlock Text="Sắp xếp:" VerticalAlignment="Center" FontWeight="SemiBold"/>
            <ComboBox Width="150" SelectedValue="{Binding SortField, Mode=TwoWay}" SelectedValuePath="Tag">
                <ComboBoxItem Content="Tên" Tag="name"/>
                <ComboBoxItem Content="Giá bán" Tag="selling_price"/>
                <ComboBoxItem Content="Ngày tạo" Tag="id"/>
            </ComboBox>
            <CheckBox Content="Tăng dần" IsChecked="{Binding IsSortAscending, Mode=TwoWay}" VerticalAlignment="Center"/>
        </StackPanel>

        <Button Content="+ Thêm sản phẩm mới" Click="OnAddItemClick"
                Style="{StaticResource AccentButtonStyle}" HorizontalAlignment="Right"/>
    </Grid>
</StackPanel>

    <!-- Danh sách -->
    <ListView Grid.Row="2"
              ItemsSource="{Binding Items}"
              BorderThickness="1" BorderBrush="Gray"
              IsItemClickEnabled="True"
              ItemClick="OnItemClick">
      <ListView.ItemTemplate>
        <DataTemplate x:DataType="m:SportItem">
          <Grid Padding="8">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="60"/>
              <ColumnDefinition Width="*"/>
              <ColumnDefinition Width="100"/>
              <ColumnDefinition Width="80"/>
            </Grid.ColumnDefinitions>
            <!-- Hình ảnh thu nhỏ nếu có path -->
            <Image Source="{x:Bind ImageUrls, Converter={StaticResource FirstImageConverter}}" Width="50" Height="50" Stretch="UniformToFill" Grid.Column="0"/>
            <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="12,0">
                <TextBlock Text="{x:Bind Name}" FontWeight="Bold"/>
                <TextBlock Text="{x:Bind Sku}" FontSize="12" Foreground="Gray"/>
            </StackPanel>
            <TextBlock Grid.Column="2" Text="{x:Bind SellingPrice, Converter={StaticResource CurrencyFormatter}}"
                       VerticalAlignment="Center" HorizontalAlignment="Right"/>
            <TextBlock Grid.Column="3" Text="{x:Bind StockQuantity}"
                       FontSize="12" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,12,0"/>
          </Grid>
        </DataTemplate>
      </ListView.ItemTemplate>
      <ListView.ItemContainerStyle>
        <Style TargetType="ListViewItem">
          <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        </Style>
      </ListView.ItemContainerStyle>
    </ListView>

    <!-- Phân trang và Lỗi -->
    <StackPanel Grid.Row="3" Margin="0,12,0,0" Spacing="8">
      <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Spacing="40">
        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Spacing="12">
          <Button Content="Trang trước" Command="{Binding PreviousPageCommand}"/>
          <TextBlock VerticalAlignment="Center">
            <Run Text="Trang "/><Run Text="{Binding CurrentPage}"/><Run Text=" / "/><Run Text="{Binding TotalPages}"/>
          </TextBlock>
          <Button Content="Trang sau" Command="{Binding NextPageCommand}"/>
        </StackPanel>
        
        <TextBlock VerticalAlignment="Center" Foreground="Gray" FontSize="12">
          <Run Text="Hiển thị "/><Run Text="{Binding DisplayFrom}"/><Run Text=" - "/><Run Text="{Binding DisplayTo}"/><Run Text=" trên "/><Run Text="{Binding TotalItems}"/><Run Text=" sản phẩm"/>
        </TextBlock>
      </StackPanel>

      <TextBlock Text="{Binding ErrorMessage}"
                 Foreground="Red" TextWrapping="Wrap" HorizontalAlignment="Center"
                 Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}"/>
    </StackPanel>
  </Grid>
</Page>
