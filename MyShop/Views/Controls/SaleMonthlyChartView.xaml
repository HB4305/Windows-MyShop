<UserControl
    x:Class="MyShop.Views.Controls.SaleMonthlyChartView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MyShop.Views.Controls"
    xmlns:lvc="using:LiveChartsCore.SkiaSharpView.WinUI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Border Background="White" CornerRadius="12" Padding="24" BorderThickness="1" BorderBrush="#E5E7EB">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <!-- Header -->
            <Grid Grid.Row="0" Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Monthly Revenue Analysis" FontSize="20" FontWeight="Bold" Foreground="#111827"/>
                    <TextBlock Text="Daily breakdown of revenue for the current month" FontSize="14" Foreground="#6B7280" Margin="0,4,0,0"/>
                </StackPanel>
                <!-- Legend mock -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Top" Margin="0,4,0,0">
                    <Ellipse Width="10" Height="10" Fill="#8b5cf6" Margin="0,0,8,0" VerticalAlignment="Center"/>
                    <TextBlock Text="Revenue ($)" FontSize="14" FontWeight="SemiBold" Foreground="#4B5563" VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
            
            <!-- Chart -->
            <lvc:CartesianChart
                Grid.Row="1"
                Series="{Binding Series}"
                XAxes="{Binding XAxes}"
                YAxes="{Binding YAxes}"
                LegendPosition="Hidden" 
                MinHeight="350">
            </lvc:CartesianChart>
        </Grid>
    </Border>
</UserControl>
